import { Callout, Tabs, Tab } from 'nextra-theme-docs'

{<h1 className="text-center font-extrabold md:text-5xl mt-8">Retes</h1>}

<div className="mx-auto max-w-full w-[880px] text-center px-4 my-4 mb-10">
  {<p className="text-lg mb-2 text-gray-600 md:!text-2xl">
    Build APIs in TypeScript for the Web Platform
  </p>}
</div>

Retes is a routing library for Node.js written in TypeScript and inspired by Clojure's Ring, Compojure and Retit. It is built directly on top of the Node.js http module, so you can use it as an alternative to Express or Koa.

## Key features 

- **Declarative**
- **Easy** 
- **Productive**
- **Typed**

## Getting Started 

Create a new project:

<Tabs items={['pnpm', 'yarn', 'npm']}>
  <Tab>
    ```bash copy
    pnpm create retes-app my-retes-app
    ```
  </Tab>
  <Tab>
    ```bash copy
    yarn create retes-app my-retes-app
    ```
  </Tab>
  <Tab>
    ```bash copy
    npx create-retes-app@latest my-retes-app
    ```
  </Tab>
</Tabs>

Install dependencies:

<Tabs items={['pnpm', 'yarn', 'npm']}>
  <Tab>
    ```bash copy
    pnpm install 
    ```
  </Tab>
  <Tab>
    ```bash copy
    yarn install 
    ```
  </Tab>
  <Tab>
    ```bash copy
    npm install
    ```
  </Tab>
</Tabs>

Start the application:


<Tabs items={['pnpm', 'yarn', 'npm']}>
  <Tab>
    ```bash copy
    pnpm start
    ```
  </Tab>
  <Tab>
    ```bash copy
    yarn start 
    ```
  </Tab>
  <Tab>
    ```bash copy
    npm run start
    ```
  </Tab>
</Tabs>